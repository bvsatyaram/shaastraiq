<% if current_user.admin? %>
  <p> Manage the following quizzes</p>
  <ul>
    <% @quizzes.each do |quiz| %>
      <li>
        <%= link_to quiz.title, quiz_path(quiz) %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>Please wait till the quiz starts</p>
<% end %>